package org.wiyn.etc.configuration;


/** This enumerated type is used for describing the known types for a source extent. It may have the values DIFFUSE (for a diffuse source) and POINT (for a point source). */
public enum SourceExtent
{
    DIFFUSE, POINT;
}
